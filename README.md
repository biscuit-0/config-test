# 测试spring-cloud-config配置中心使用

```
echo "# config-test" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/biscuit-0/config-test.git
git push -u origin main
```

